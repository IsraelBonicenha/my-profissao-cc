import ApplicationPage from "./application";

export const metadata = {
  title: "commitGenerator",
  description: "Transforme diffs em commits claros e padronizados.",
};

export default function Page() {
  return <ApplicationPage />;
}
